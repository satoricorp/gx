---
title: "Totality changelog: new features, updates, and fixes"
description: "Changelog for Totality covering new features, product updates, CLI improvements, review workflow changes, and bug fixes."
---

## July 29, 2026

**PR summary verdict labels** — The summary's triage line now states what the reader
must do, not just a color: 🟢 **Quick scan** (LOW), 🟡 **Careful pass** (MEDIUM),
🔴 **Deep review** (HIGH). The label is enforced server-side to always match the
Blast Radius level, so a red summary can never read as permission to skim.

**Totality** — The product formerly known as GX is now Totality. The CLI is `tl`
(`tlr` for one-shot review, `tl-mcp` for agents). Install from
`download.totality.sh`; docs live at `docs.totality.sh`.

## July 26, 2026

**Plain Git is the whole workflow** — Capture and publishing are entirely hook-driven:
`git commit` records a revision, `git push` captures the session and publishes. The
porcelain that duplicated Git (`tl push`, `tl sync`, the background daemon, and the
macOS menu-bar app) has been removed. If you have an older install, `tl init` cleans
up the retired pieces.

**PR summaries write to the description** — Totality Cloud writes the rich summary
into the pull request description below your own text, behind a Totality-owned
marker. Your notes survive every regeneration.

**`tl review` is guaranteed read-only** — Review writes nothing: no repo state, no
`.git/index` mutation, no `~/.totality` on machines that never ran `tl`, and no
uploads when a gate fails. `--fail-on` turns a review into a CI gate with distinct
exit codes for "findings survived" and "nothing was reviewed". See the
[CLI reference](/docs/cli#tl-review).

**Two-model review panel** — `tl review` runs two independent Anthropic reviewers
plus a judge that verifies and dedupes findings. When both reviewers flag the same
issue independently, the report says so — the strongest confidence signal a panel
produces.

## July 2, 2026

**Generated GitHub PR summaries** — After you publish with plain `git push` and open
a PR, Totality Cloud posts a generated review summary highlighting blast radius and
high-impact review targets.

**Code review history memory** — `tl review` pulls in indexed context from your past
reviews and GitHub comment history, and attaches review policy context so findings
stay consistent with how your team reviews code.

**Prompted `tl review`** — Trigger `tl review` with a targeted prompt to focus the
review on a specific concern instead of the full default sweep.
