.DS_Store
.env
.env.local
.totality/
/tx
dist/
node_modules/
mcp/.xmcp/
mcp/.*.bun-build
mcp/dist/
mcp/node_modules/
mcp/xmcp-env.d.ts
.gstack/

# Personal Claude Code settings. The shared .claude/settings.json carries the
# Totality capture hooks; this one is per-developer and must never be committed.
.claude/settings.local.json

# Fumadocs site (docs-site/) generated output
docs-site/node_modules/
docs-site/.next/
docs-site/.source/
